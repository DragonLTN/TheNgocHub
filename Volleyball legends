local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Lấy key tự động từ một URL do bạn quản lý
local currentKey = "TheNgocDefault" -- Giá trị dự phòng nếu không kết nối được
local success, result = pcall(function()
    return game:HttpGet("https://raw.githubusercontent.com/DragonLTN/TheNgocHub/refs/heads/main/Key")
end)

if success and result then
    -- Xóa bỏ các ký tự xuống dòng thừa nếu có
    currentKey = string.gsub(result, "%s+", "")
end

local Window = Rayfield:CreateWindow({
    Name = "TheNgocHub",
    LoadingTitle = "By Thế Ngọc",
    LoadingSubtitle = "chúc quý khánh sử dụng vv",
    ConfigurationSaving = { Enabled = true, FileName = "Config" },
    KeySystem = true,
    KeySettings = {
        Title = "Key System",
        Subtitle = "Nhập key để tiếp tục",
        Note = "Key lấy ở TikTok @dragonltn",
        FileName = "Key",
        SaveKey = false,
        Key = {currentKey} -- Sử dụng biến key động vừa lấy về
    }
})

task.spawn(function()
    task.wait(0.5)
    loadstring(game:HttpGet("https://pastefy.app/MSEHvrai/raw"))()
end)

local Tab = Window:CreateTab("Trang chủ", 4483362458)

Tab:CreateButton({
    Name = "Buff Spin",
    Callback = function()
        loadstring(game:HttpGet("https://pastefy.app/MSEHvrai/raw"))()
    end,
})

Rayfield:LoadConfiguration()
